#include "main.h"

/**
 * _atoi - convert a string characters to integers.
 * @s: the string to be worked on.
 *Return: The integer value of the comverted string.
 */

int _atoi(char *s)
{
	return (0);
}
