# Build all c files stored in a variable SRC
# The compiler must be stored in a variable CC
# The `all` rule builds the executable
# The all rule should recompile only the updated source files
CC = gcc
SRC = school.c main.c
NAME = school
OBJ = $(SRC:%.c=%.o)

all: $(OBJ)
	$(CC) $(OBJ) -o $(NAME)
